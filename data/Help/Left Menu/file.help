It it in the left-top corner.

There is a lot of buttons that you can call with hot keys. With them (buttons), you can work with file, clear the memory.
There is source master (to work with sourses which are using in you html-page), link master (to work with links which are using in you file).
There is input field so you can download site from it.